# 7BB1002_group4
# This repository includes all code used for reproduce the RNA-seq section of the paper on HPC and in R.
# To build environment, read the team set up part.
# use envbuilding.sh to build the environment, and the version of packages is in the yeast_rnaseq_cli.yml and R_env.yml(If you want to use R on HPC, we provide this)
